export enum UserRole {
  READER = 'reader',
  WRITER = 'writer',
  ADMIN = 'admin',
}

export enum StatusType {
  SUCCESS = 'success',
  INFO = 'info',
  ERROR = 'error',
}

export enum PostState {
  DRAFT = 'draft',
  PUBLISHED = 'published',
}
